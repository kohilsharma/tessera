# Dependencies
node_modules/

# Build output
dist/
build/
.vite/

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Test / coverage
coverage/

# Local scratch / tooling
.scratch/
*.local

# Stale Python venv (project is Node — see ADR-0005)
backend/.venv/

# Local disk storage for uploaded Brief cover images (ADR-0015)
backend/uploads/

# OS
.DS_Store
Thumbs.db

# Claude Code tooling / installed skills (reproducible via skills-lock.json)
.claude/
.agents/
.codex/
.impeccable/
.playwright-mcp/

# Agent tool state (regenerated on activation)
.serena/
.hallmark/

# TS incremental build cache
*.tsbuildinfo

# Installed skill payloads (reproducible via skills-lock.json, like .agents/)
agent/

# Design-exploration screenshots (kept locally, not in history)
shots/

semantic-review/
uploads/

# --- Kept locally, out of the published repo -------------------------------
# Working notes and tooling for the AI-assisted workflow. These are how the work
# was done, not what was built; the repo carries the result and the ADRs behind it.
AGENTS.md
CLAUDE.md
skills-lock.json
docs/agents/
docs/repo-state.md
docs/repo-state/

# Internal planning and exploration, superseded by the ADRs and the README.
PRODUCT.md
docs/design-brief.md
docs/phase-3.6-spec.md
docs/research/
docs/Tessera Directions.html
docs/architecture/*.json

# QA evidence and course submission artifacts — large, and neither is source.
docs/verification/
project-docs/

# Personal recording script.
docs/demo-runbook.md
